execution:
  mode: rest_sync           # switch to 'stream' when connecting to Next.js SSE frontend

session:
  type: in_memory           # upgrade to 'redis' for production persistence

multimodal:
  enabled: false
  storage_type: in_memory
  max_attachments: 5

logging:
  level: INFO
